# Core ML stack (CPU/MPS compatible)
torch              # Conda install recommended for MPS/CPU build
einops
tqdm
coolname
pydantic
argdantic
wandb
omegaconf
hydra-core
huggingface_hub
packaging 
ninja 
wheel 
setuptools 
setuptools-scm
pydantic-core
numba

# Removed or replaced:
# adam-atan2 ->  CUDA-only extension, omit entirely
# triton ->  Not supported on macOS, skip unless needed for CPU fallback

# Optional
# xformers  # (Apple MPS–compatible attention ops, available via pip)